// firebaseConfig.ts
// Firebase configuration temporarily disabled
// import app from '@react-native-firebase/app';
// import messaging from '@react-native-firebase/messaging';
// import firestore from '@react-native-firebase/firestore';

// React Native Firebase automatically initializes when imported
// No need for manual initialization with @react-native-firebase/app

// export { app, messaging, firestore };

// Temporary exports to prevent import errors
export const app = null;
export const messaging = null;
export const firestore = null;
